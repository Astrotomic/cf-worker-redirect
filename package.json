{
	"name": "cf-worker-redirect",
	"version": "2.0.0",
	"main": "src/index.js",
	"scripts": {
		"dev": "wrangler dev",
		"build": "webpack",
		"deploy": "worker-setup deploy",
		"publish": "wrangler publish",
		"generate": "worker-setup generate && cp redirects.example.json redirects.json"
	},
	"license": "MIT",
	"devDependencies": {
		"@cloudflare/workers-types": "^2.2.2",
		"webpack": "^5.58.2",
		"webpack-cli": "^4.9.0",
		"worker-setup": "^1.5.0"
	}
}
